// here we import an object from the module react
import React from 'react';
import ReactDOM from 'react-dom';

const element = < h1 > React without Reddux < /h1>;
ReactDOM.render(element, document.getElementById('root'));